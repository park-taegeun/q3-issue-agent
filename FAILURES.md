# FAILURES.md

구현 중 실패, 오탐, 잘못된 가정과 그것을 어떻게 발견하고 고쳤는지 기록한다.
특히 **LLM(Claude) 판단이 규칙/데이터와 어긋난 사례**를 1급 기록으로 남긴다 —
"LLM은 규칙 설계에만 개입하고 수치·선택은 코드가 한다"는 경계를 어디서 지켰고 어디서 흔들렸는지 포함.

---

## 2026-07-30 — 잘못된 가정 #1: "함정은 집계 지표를 안 움직일 것이다"

**가정**: T1(5km+ 노쇼 0.12→0.28)을 심어도 근거리 노쇼를 -1%p 개선해 두면 전체 노쇼율은 거의 안 움직일 것.

**실제**: 반경 완화로 5km+ 매칭 비중이 7%→14%로 커지면서 전체 노쇼율이 7.8%→10.6%(+2.8pp, 상대 +36%)로 올랐다. "집계는 멀쩡해 보인다"는 함정의 전제가 깨졌다.

**발견 경로**: metrics.py 첫 실행에서 overall 수치를 눈으로 확인하다 발견.

**수정 (2가지)**:
1. 근거리 노쇼 개선을 -2%p로 키워 오염을 완화 (전체 +1.8pp 수준 = "반경을 완화했으니 이 정도는" 하고 넘어갈 만한 수준).
2. 더 중요하게 — 탐지 규칙을 "전체 지표가 멀쩡할 것"에 의존하지 않게 설계 변경: 후보 조건을 '전체가 정상'이 아니라 **세그먼트 악화 − 전체 악화의 초과분(excess)**으로 정의. 현실에서도 곪은 세그먼트는 집계에 일부 새어 나오므로, '괴리'가 본질이지 '집계의 무결'이 본질이 아니다.

## 2026-07-30 — 잘못된 가정 #2: "재이용률은 다른 지표와 독립일 것이다"

**가정**: 재이용률(세그먼트·기간 내 2회 이상 매칭된 worker 비율)은 함정을 안 심었으니 안정적일 것.

**실제**: T2(야탑동 매칭 급감)의 부수효과로 야탑동 재이용률이 0.434→0.232로 동반 붕괴했다. 매칭 총량이 줄면 worker당 매칭 횟수가 기계적으로 줄어드는, 지표 정의 자체에 내장된 종속성이었다. 탐지기가 이걸 별도 문제로 올리면 오탐이 된다.

**수정**: 같은 세그먼트에서 여러 지표가 동시에 악화하면 퍼널 상류 지표(전환율)를 대표 후보로 남기고 하류 지표(재이용률)는 "상류 악화의 기계적 파생"으로 폐기하는 dedup 규칙을 추가. 지표 정의를 바꾸는 대신 규칙으로 통제한 이유: worker별 시간 윈도우 기반의 '진짜' 재이용률은 이 프로젝트 범위를 넘는다.

## 2026-07-30 — LLM 개입의 한계 사례 #1: 정답을 아는 채로 임계값을 '정답에 맞추려' 했다

**무슨 일**: sequential-thinking으로 탐지 임계값을 설계하던 중(사고 2단계), n=50에서 노이즈 여유를 근거로 절대폭 임계를 5pp→8pp로 올리는 안을 검토하다가 "8pp면 T2(−8.0pp)가 경계선에 걸려 미탐될 수 있다"는 이유로 임계를 다시 조정하려 했다. 이것은 **함정의 정답 크기를 아는 LLM이 규칙을 정답에 맞추는 순환** — 채점자가 정답을 알면 유리하게 맞추게 된다는, 이 프로젝트가 격리로 막으려던 바로 그 오류를 규칙 설계 단계에서 내가 저지를 뻔했다.

**어떻게 잡았나**: 다음 사고 단계에서 "임계는 함정과 무관한 원칙에서 나와야 한다"는 자기 검증에 걸렸다. 최종 임계(절대폭 5pp)는 제품 기준(매칭 퍼널에서 5pp는 액션을 요하는 크기)에서 도출했고, 노이즈 통제는 표본 조건(MIN_N=50)에 역할을 분리했다.

**경계 평가**: "LLM은 규칙 설계에만 개입, 수치·선택은 코드가"의 경계 자체는 지켜졌다(수치 계산·최종 선택은 전부 코드). 흔들린 것은 규칙 설계 단계 안에서의 독립성이었다 — 사람이 설계했어도 똑같이 저지를 수 있는 오류지만, LLM은 생성기 코드를 직접 봤기 때문에 위험이 더 크다. 향후 임계 조정은 '실측 오탐/미탐'(multi-seed 평가 결과)을 근거로만 한다.

## 2026-07-30 — LLM 개입의 한계 사례 #2: "다른 축으로는 안 샐 것"이라는 예측이 데이터로 반증됨

**무슨 일**: 규칙 설계(sequential-thinking 사고 5단계)에서 "거리 함정이 지역 축으로 새어 보일 가능성은 희석 때문에 낮다. 규칙으로 강제하지 않고 실측으로 확인한다"고 판단했다.

**실제**: 5시드 평가에서 오탐 2건(시드 44: 백현동 노쇼, 시드 45: 주방 노쇼)이 나왔고, 분해해 보니 둘 다 **T1(5km+ 노쇼)이 구성비 효과로 다른 축에 새어 나간 것**이었다(백현동 current 노쇼의 절반이 5km+ 매칭 26건 중 13건). 작은 세그먼트에서는 희석돼도 노이즈와 겹치면 임계를 넘고, 심지어 점수 1위까지 했다.

**수정**: 직접 표준화(direct standardization) 검사를 dedup 0단계로 추가 — 후보 세그먼트의 current 실제값이 '통제축 구성비 × 전체 통제축별 비율'로 만든 기대값과 5%p 미만 차이면 "구성비로 설명됨"으로 폐기. 함정 위치 지식 없이 탐지기 데이터만으로 계산하므로 격리 원칙과 무관. 수정 후 동일 시드에서 오탐 0.

**교훈**: LLM의 "그럴 가능성은 낮다"는 직관은 검증 전까지 가설일 뿐이다. 실측(multi-seed)이 있었기에 잡혔다.

## 2026-07-30 — LLM 개입의 한계 사례 #3: "통계 검정은 과하다"는 설계 판단이 반증됨 (2단 실패)

**1단**: 규칙 설계 때(사고 2~3단계) "신뢰구간 기반 검정은 복잡도만 늘린다. 고정 임계 + 표본 하한(MIN_N=50)이면 충분하다"고 판단했다. 그러나 **함정 없는 클린 데이터** 참음성 검사에서 10시드 중 5회 헛제안이 나왔다. 원인은 다중 비교: 세그먼트×지표로 회당 100건 넘는 검정을 하니 2.5~3σ 우연이 매 실행 몇 개씩 생기고, 함정 데이터에서는 진짜 함정에 점수로 밀려 안 보였을 뿐이었다.

**2단**: 이를 Bonferroni 보정으로 고쳤더니 클린 헛제안은 0이 됐지만 primary 적중이 14/15 → 9/15로 추락했다. T1의 실현 신호(z≈3.0~3.3)가 정확히 Bonferroni 임계(z≈3.35)에 걸렸다. FWER 통제는 '발견' 문제에 과보수적이라는 교과서적 함정을 그대로 밟았다.

**최종 수정**: Benjamini-Hochberg(FDR, α=0.05)로 교체. 강한 진짜 신호(T2, p~1e-20)가 있으면 임계가 적응적으로 올라가 경계 신호(T1)를 살리고, 신호 없는 데이터에서는 최솟값 검정이 Bonferroni와 같아져 헛제안 0 유지. 결과: 함정 15시드 정탐 15/오탐 0, 클린 20시드 헛제안 0.

**경계 평가**: 두 차례 모두 조정의 근거는 심은 정답이 아니라 **실측된 오탐/미탐**이었다 — 사례 #1에서 세운 원칙이 지켜졌다.
