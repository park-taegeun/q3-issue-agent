# DECISIONS.md

설계 결정과 그 근거, 폐기한 대안, 막힌 지점을 시간순으로 기록한다. 되돌린 결정도 지우지 않는다.

---

## 2026-07-30 — 프로젝트 구조: 생성기/탐지기/채점기 3분할

**결정**: `src/gen/`(함정을 심는 쪽), `src/agent/`(찾는 쪽), `score.py`(채점)를 물리적으로 분리한다. 함정 파라미터는 `src/gen/traps.py`에만 존재하고, `answers/ground_truth.json`은 score.py만 읽는다.

**근거**: 채점자가 정답을 알면 유리하게 맞추게 된다. 탐지기가 정답에 접근할 수 없어야 "잡았다"는 결과를 신뢰할 수 있다. score.py가 실행 시 `src/agent/` 소스에 `ground_truth`/`answers`/`traps` 문자열이 없는지 정적 검사하는 가드를 추가해 이 격리를 코드로 강제한다.

**폐기한 대안**: 단일 패키지 안에서 "탐지 코드가 정답을 import하지 않기로 약속" — 사람이 약속을 지키는 구조는 검증이 불가능해서 버렸다.

## 2026-07-30 — 의존성: 파이썬 표준 라이브러리만 사용

**결정**: sqlite3, random, json, datetime, argparse만 쓴다. venv/requirements 없음.

**근거**: 목적이 뾰족한 CLI 하나다. pandas를 넣으면 편하지만 설치 단계가 생기고, "한 줄 명령 재현"이 환경에 의존하게 된다. 이 규모(매칭 ~1.2만 건)에서 표준 라이브러리로 충분하다.

## 2026-07-30 — 기간 축: 8주를 전반 4주(baseline) vs 후반 4주(current)로 고정 분할

**결정**: 2026-06-01 ~ 2026-07-26의 8주를 절반으로 나눠 기간 비교한다.

**근거**: "집계 지표는 개선처럼 보이는데 세그먼트가 곪았다"는 함정은 시간 비교 없이는 성립하지 않는다(개선 = 이전 대비). 이동 윈도우나 주 단위 추세 분석은 범위 밖 — 비교 축은 하나면 된다.

## 2026-07-30 — 이름 계약(contract.py)을 3자 공유로 허용

**결정**: 축 이름, 지표 이름, 거리 구간 경계 같은 **어휘**만 담은 `src/contract.py`를 생성기·탐지기·채점기가 공유한다.

**근거**: 채점은 "탐지기의 제안 (축, 값, 지표)"과 "정답의 (축, 값, 지표)"의 문자열 대조다. 어휘가 다르면 정탐도 오탐으로 채점된다. 함정의 위치/강도 같은 **정보**는 계약에 없으므로 격리 원칙과 충돌하지 않는다.

**폐기한 대안**: 채점기에 퍼지 매칭(비슷한 이름 인정)을 넣는 것 — 채점 로직이 애매해지면 결과도 애매해진다.

## 2026-07-30 — 함정은 핵심 3개, 심슨의 역설(mix-shift)은 stretch로 보류

**결정**: 거리(primary)·지역·직종×요일 3개로 먼저 완성한다. 심슨의 역설 함정은 핵심 3개가 multi-seed에서 정탐되고 오탐이 통제된 뒤에만 추가 검토하고, mix-shift를 위해 핵심 탐지 로직을 미리 복잡하게 만들지 않는다.

**근거**: 뾰족한 핵심 하나(평균에 가려진 세그먼트 악화 탐지)를 확실히 하는 것이 목적. 사용자 지시로 범위 확정 (2026-07-30). 참고로 dedup 0단계의 직접 표준화 검사가 구성비 효과를 이미 다루므로, mix-shift 함정을 넣게 되면 이 검사와의 상호작용을 먼저 설계해야 한다.

## 2026-07-30 — 탐지 규칙: '전체가 멀쩡한가'가 아니라 '초과 악화(excess)'를 본다

**결정**: 후보 조건 = [세그먼트 악화 절대폭 ≥ 5pp] ∧ [전체 악화 대비 초과분 ≥ 5pp] ∧ [상대 악화 ≥ 30%] ∧ [두 기간 표본 ≥ 50] ∧ [유의성 통과]. 임계값은 심은 함정이 아니라 제품 기준(5pp = 액션을 요하는 크기)에서 도출했다 — 이 도출 과정에서 순환에 빠질 뻔한 기록은 FAILURES.md 참고.

**근거**: 곪은 세그먼트는 집계에도 일부 새어 나온다(실측: T1이 전체 노쇼를 +2pp 움직임). '전체 무결'을 요구하면 진짜 문제일수록 놓친다. 괴리가 본질이다.

## 2026-07-30 — 심각도 점수: 상대 악화 × 볼륨 비중 × 지표 가중치

**결정**: 점수 세 요소를 곱한다. 볼륨 비중은 '같은 지표의 분모 대비'로 계산해 지표 간 단위를 일치시킨다. 지표 가중치는 노쇼 1.5 > 재이용 1.3 > 전환 1.2 > 1시간 1.0 — 노쇼는 성사된 약속의 파기라 신뢰 훼손의 질이 다르다는 판단. 가중치는 '판단'이므로 proposal.json에 그대로 노출해 리뷰 가능하게 한다.

**폐기한 대안**: 피해 건수(악화폭×분모) 기반 점수 — 지원 수 분모인 전환율이 매칭 수 분모인 노쇼율을 볼륨으로 항상 압도해 불공정했다.

## 2026-07-30 — dedup 3단: 표준화 검사 → 퍼널 상류 우선 → 축 중복 제거

**결정**: (0) 직접 표준화 — 후보의 current 실제값이 통제축 구성비 기대값과 5pp 미만 차이면 '구성비 효과'로 폐기. (1) 같은 세그먼트 다중 지표 악화 시 퍼널 상류(전환→1h→노쇼→재이용) 대표. (2) 2차원 축과 성분 축의 같은 지표는 점수 높은 쪽만.

**근거**: 전부 실측에서 나왔다 — (0)은 오탐 2건(T1의 타 축 누출), (1)은 야탑동 재이용률 동반 붕괴(FAILURES.md 참고).

## 2026-07-30 — 유의성 필터: Bonferroni를 썼다가 BH(FDR)로 되돌림 (번복 기록)

**결정**: 2-비율 z-검정 단측 p값에 BH(α=0.05) 보정. **한 번 번복했다**: 처음엔 보정 없이 갔다가(클린 데이터 10시드 중 5회 헛제안) Bonferroni를 넣었고(헛제안 0, 그러나 primary 적중 14/15→9/15 추락), 최종적으로 BH로 완화했다(헛제안 0 유지 + primary 회복). 전 과정은 FAILURES.md #3에.

**근거**: 이 문제는 '발견(discovery)' — 통제해야 할 것은 FWER가 아니라 FDR이다. BH는 강한 진짜 신호가 있으면 임계를 적응적으로 올려 경계 신호를 살리고, 신호가 없으면 Bonferroni처럼 조여진다.

## 2026-07-30 — primary 적중은 참고 지표, 정탐이 성공 기준

**결정**: 시드에 따라 함정의 '실현' 강도가 다르다(예: 시드 46은 T1의 baseline 노쇼가 우연히 높게 뽑혀 실현 신호가 약함). 그 경우 탐지기가 실현 데이터 기준으로 더 심각한 다른 함정(T2)을 고르는 것은 올바른 동작이다. 채점은 '심은 함정 중 하나를 근거와 함께 잡았는가(정탐)'를 성공으로, primary 적중률은 참고로 본다.
